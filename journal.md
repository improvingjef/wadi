## 2026-03-28

- `ocamldep -sort` is enough to remove hand-maintained intra-target module ordering as long as the builder still keeps `main` last for runnable link order.
- Content digests on manifest, source files, dependency stamps, and toolchain facts are a much more trustworthy incremental signal than timestamps alone.
- Package support is not just `-package foo`; the build has to propagate external package requirements through workspace-library dependencies or downstream targets fail at compile/link time.
- Relying on `PATH` for `ocamlfind` is brittle even inside an opam switch. The builder now has to resolve tool locations intentionally instead of assuming shell state is correct.
- Every new source module still requires touching the bootstrap `Makefile`, which reinforces that task 25 is not cleanup vanity work; it is active friction on the project.
- Resolving `ocamlc -where` and then locating `unix.cmi` in either the stdlib root or a `unix/` subdirectory removes one more ambient toolchain assumption from the bootstrap path.
- A single command table is not just a cleanup refactor; it gives the CLI one source of truth for help text, parsing entry points, and dispatch, which is the prerequisite for generated docs and completions later.
- A dedicated layout module removes `_oasis` path drift across build, clean, tests, and future install/diagnostic subtools; the path scheme only counts as simple if it lives in one place.
- `oasis toolchain` is much more useful when it reports resolved executable paths and package roots, not just the configured command tokens; debugging switch issues starts with concrete paths.
- Driving bootstrap compilation from a real root `oasis.toml` removes the duplicated object graph, but it also exposed that the self-hosting path still lags the main builder on `.mli` handling and package-derived flags.
- A clean target that regenerates build metadata before deleting it is the exact kind of pointless friction that gives build tools a bad name; bootstrap plumbing has to respect lifecycle semantics too.
- Backend selection has to be a first-class concern, not an accidental consequence of whichever compiler happens to be installed; `auto|native|bytecode` only became trustworthy once the builder, toolchain report, and bootstrap path all resolved through the same switch.
- Generic bootstrap pattern rules looked simple, but they were hiding two real correctness gaps: `.mli` files need their own build edges, and package flags belong to specific target groups rather than a global `unix` escape hatch.
- Rebuilding `_bootstrap` from scratch on every `make test` is cheap insurance; self-hosting claims are not credible if the fast path never proves that generation, compilation, and linking still compose from an empty directory.
- Generating explicit bootstrap compile rules exposed a remaining sharp edge: a shared object directory means duplicate module stems across app/test/library slices should be rejected early instead of producing confusing overwrites.
- Generated files only feel civilized when they stay under `_oasis`; copying back only declared action outputs keeps sandboxes useful and avoids dirtying the source tree with codegen fallout.
- Sandbox execution immediately exposed a boring but critical filesystem detail: copied helper scripts have to preserve executable bits or the whole “declarative action” story collapses on first use.
- Profiles need to change artifact roots, not just flags. Reusing one build directory across `release` and `dev` would turn “profile” into a cache-corruption feature.
- Text preprocessors fit naturally as stdin/stdout transforms, while PPX support is cleaner when the builder treats rewriters as named tools and lets the compiler own the AST pipeline.
- Growing the manifest model triggered a wave of OCaml record-label ambiguity; explicit type annotations are a small price to pay for keeping a richer configuration format readable and type-safe.
- `ocamlfind printconf path`, `ocamlfind query ...`, and `ocamlc -where` were quietly happening far more often than the user-visible work justified; a per-build toolchain session buys real speed by collapsing those probes to one hit.
- Human-facing rebuild explanations only stay trustworthy if downstream fingerprints summarize dependencies at the dependency edge. Embedding raw dependency fingerprint text made `oasis explain` blame leaf source files in the wrong target.
- A cached target still needs first-class diagnostics. Persisting `.oasis-explain` next to `.oasis-stamp` turns “why did this rebuild?” from guesswork into a cheap read instead of another compile.
- The bootstrap path still has one embarrassing duplication seam: the generated makefile is manifest-driven, but `scripts/generate_bootstrap_makefile.ml` still carries a hand-maintained `#mod_use` list for core modules.

## 2026-03-29

- `oasis install` only feels real once it stages more than a binary. Copying compiler-consumable library artifacts, the source manifest, and a machine-readable install manifest turns a build output into something another tool can reason about.
- A command table is not actually a single source of truth if it only feeds `--help`. Generating markdown docs and shell completions from the same metadata is what makes the subtool split discoverable instead of aspirational.
- Persisting `.oasis-explain.json` beside the human report is cheap and worthwhile as long as both formats are emitted from the same builder data. Two renderers over one fact set are maintainable; two diagnostic code paths are not.
- The next install friction showed up immediately: local staging is pleasant, but downstream packaging still wants `META` export and `DESTDIR`-style relocation.
- Static shell completion is enough to prove the command-table design, but the moment the tool grows real workspace nouns, target-aware and profile-aware completion becomes part of usability rather than polish.
- `META` generation has to come from manifest intent, not just whichever artifacts happened to land in `lib/<name>`. The useful signal is direct workspace-library deps plus external package deps.
- `DESTDIR` is a two-path problem: package managers care about the logical prefix, while the filesystem cares about the realized stage root. Install metadata has to record both or the staging story stays ambiguous.
- `oasis explain --json` is most trustworthy when it prints the persisted payload directly. Automation should consume the exact sibling file the builder wrote, not a second renderer that might drift.
- macOS temp paths quietly canonicalize through `/private`; staging tests that care about installed roots need to compare normalized paths, not raw temp-directory strings.
- A useful dry-run diagnostic is just the build planner with the compiler and linker switched off. `oasis explain --current` only became credible once it reused the same fingerprint, dependency, and command-planning path as a real build.
- Relative `--prefix` plus `--destdir` needs two truths at once: preserve the logical prefix the user asked for, and stage under the physical root the filesystem needs. Rewriting `_stage` into an absolute workspace path is exactly the kind of packaging surprise that makes build tools feel hostile.
- Bootstrap generation has to reject duplicate module stems before it writes rules. A shared `_bootstrap/obj` directory turns same-stem library/app/test modules into silent overwrites unless the planner names the collision up front.
- Multi-package support stays understandable when the root manifest owns workspace-wide defaults, profiles, and tool registries while member manifests contribute rebased targets. Letting every package define its own workspace-level knobs immediately turns merge order and relative-path semantics into guesswork.
- `oasis install` has two separate notions of selection: what the user explicitly asked for and what must be staged so internal `META` references stay valid. Treating those as the same set quietly produces broken staging outputs.
- Dry-run diagnostics need the same backend control as real builds. If `oasis explain --current` cannot say “show me bytecode planning” or “show me native planning,” it is still leaking ambient toolchain state into something that should be inspectable on purpose.
- Generated sources cannot be allowed to silently outrank checked-in modules. Failing fast on `version.ml`-style collisions is much less hostile than compiling the wrong file and pretending nothing strange happened.
- Preprocessors and PPX rewriters stop feeling magical once their sidecar inputs are declared and fingerprinted. A tiny `deps = [...]` list turns “why did this rebuild?” from a guess into a direct answer.
- Install metadata needs provenance, not just inventory. Package tooling cares whether an artifact was explicitly requested or staged because another target’s `META` would otherwise dangle.
- A dry-run that still runs actions or preprocessors is fake. Keeping `oasis explain --current` honest required planning around virtual generated/preprocessed paths and accepting declared-order fallbacks when source transforms were intentionally not materialized.
- Steady-state diagnostics need the declared pipeline shape, not just invalidation blame. Seeing action outputs plus action/preprocessor/PPX `deps` in persisted `oasis explain` output makes generated-source stacks inspectable before anything changes.
- Workspace-aware completion is immediately better when it reads the manifest, but generation-time completion snapshots are still a compromise; the clean endgame is a runtime completion query protocol that follows the current workspace automatically.
- Manifest-driven bootstrap loading only works if the scanner treats every new section header as a hard boundary. Otherwise a helper meant to load core modules can silently drift into executable/test sections and bootstrap the wrong code.
- A hidden runtime completion query fixes stale workspace nouns, but it also exposes a follow-up ergonomics gap: directory-valued flags like `--workspace` and `--prefix` need shell-native path completion or the UX still regresses.
- Dry-run explain has to discount generated-source fingerprints that are only missing because actions were intentionally not materialized. Without that normalization, “rerun the action and reuse the binary” looks indistinguishable from a full rebuild.
- Package-local helper tools only stay local if lookup is scoped by the target’s package and only falls back to the root manifest on purpose. Naively merging member actions/preprocessors/PPX into one flat namespace would just recreate the centralization pressure in another form.
- Member-path rebasing needs normalization, not just prefixing. Leaving `./scripts/tool.sh` as `packages/core/./scripts/tool.sh` is technically valid but leaks ugly paths into diagnostics and tests immediately.
- Package-aware completion becomes materially more useful once the query protocol can emit descriptions. zsh and fish can surface member paths today; bash still needs a shell-specific fallback instead of pretending plain-word completion is enough.
- Migration docs and manifest reference examples are not optional polish once actions, preprocessors, PPX `deps`, and generated-source collision rules become explicit. The whole point is to make the sharp edges legible before the first failed build.
- A runtime completion query needs one more bit than “here are the words”: it also has to say when the shell should stop asking for logical nouns and hand control back to native directory completion for path-valued flags.
- Bash is the awkward shell in the set. It can complete the right target names easily, but surfacing package-path context without corrupting the inserted word requires a fallback display path separate from the actual completion values.
- Package-path annotations only feel consistent once every user-facing subtool carries them through its own summaries. Build output alone is not enough when `run`, `test`, and `install` are the commands people actually live in.
- A completion protocol only becomes reusable once it has an explicit versioned header. The old single magic marker worked for one branch, but fish and zsh both got simpler once the response said “protocol/version/kind” up front.
- Shell completion tests that never execute the generated scripts are wishful thinking. The fish bug was a real tab-parsing mistake that snapshot tests happily blessed and a runtime test caught immediately.
- Shipping docs and completions as committed artifacts is only defensible if one script regenerates them and CI diff-checks the result. Otherwise “generated from the live command table” is just marketing copy.
- The new release-artifact path exposed one more coupling smell in the bootstrap layer: refreshing the CLI binary for docs/completions still walks test sources, which means unrelated test syntax can block publishing command metadata.
- Bootstrap parity only gets believable once the generated makefile owns its own manifest-driven dependency list. Making every object rule depend on `$(BOOTSTRAP_MK)` turns profile, action, preprocessor, and PPX changes into normal rebuilds instead of hidden state.
- Splitting bootstrap generation into app-only and full scopes is what finally decouples release-artifact refreshes from unrelated test breakage. The broad `test/*.ml` wildcard was the exact kind of ambient coupling that makes build tools feel arbitrary.
- Workspace sandboxes do not need weaker isolation to get faster. Swapping byte-for-byte copies for clone-on-write file materialization keeps undeclared writes contained while avoiding the worst of large-tree copy costs.
- Shell quoting rules for env assignments are different from shell quoting rules for argv. Quoting `BUILD_PROFILE=release` as one opaque token quietly turns it from an environment binding into a command name.
- Recursive `make` inside tests is not hermetic by default. Clearing inherited `MAKEFLAGS` and pinning tool variables in the harness keeps bootstrap smoke tests about the generated makefile instead of the parent build environment.
- The cleanest way to add inspection subtools is to reuse the real build planner. `oasis graph` only became trustworthy once it asked the same planner for module order and pipeline shape instead of inventing a second explanation path.
- External dependency reporting has to follow workspace-library closure, not just target-local `packages = [...]`. If `demo` depends on `core` and `core` brings `unix`, `oasis deps demo` has to say `unix` directly or it recreates the same hidden-state problem people hate in Dune.
- A practical migrator needs source-tree inference as well as stanza parsing. Reading `dune` forms alone is not enough when executables omit `modules`; scanning `.ml` and `.mli` files is what turns a first-pass manifest from theory into something the user can actually build.
- Once the command table grows, committed docs and completions stop being “generated artifacts” and become part of the compatibility surface. The sync test failing immediately was a useful reminder that discoverability has to ship alongside the code, not one commit later.
- A self-hosted bootstrap planner does not have to land as an all-or-nothing rewrite. Using the interpreter only to seed the app binary and then asking the compiled `oasis` binary to generate the full makefile removes the most painful drift without blocking on a perfect cold-start story.
- `public_name` is not migration trivia. The moment the migrator emits it as a real manifest field, the installer and `META` generator also have to honor it or the staged tree lies about what users will actually consume.
- Translating dune `pps` is much cleaner when the migrator resolves them once through `ocamlfind printppx` and serializes an explicit `ppx.*` argv. That turns an implicit package-level behavior back into concrete build input.
- `oasis env` became useful only when it printed the full inherited subprocess environment, not just manifest deltas. The tradeoff is obvious immediately: exactness is valuable, but it creates pressure for a follow-up changed-only or JSON mode.
- `oasis env --json` was almost free once the report model already carried structured contexts. The important design constraint is one fact set with two renderers, not a second data path for “machine-readable mode.”
- A package-aware OCaml REPL still needs runtime `-I` search paths even after `ocamlmktop` links the workspace libraries. Without those include dirs, script files and typed phrases fail on unbound modules despite a successful link.
- OCaml’s `-init` flag is a poor foundation for automated REPL tests. Script-file execution is the dependable noninteractive path, which means the repl subtool needs an explicit story for interactive and scripted use instead of assuming the toplevel treats them the same.
- Dune migration gets materially better the moment the tool stops warning on forms it can actually read. Inferring `deps` from explicit file arguments in simple `run` and `copy` actions removes busywork without weakening the “be explicit” model.
- Wrapped-library support only counts if the builder, bootstrap planner, and migrator all agree on the same synthetic wrapper module. Landing it in only one path would recreate the exact “special case drift” that makes Dune feel opaque.
- A generated namespace wrapper has to reserve the library-name stem up front. Letting checked-in `core.ml`, action outputs, and an auto-generated wrapper compete for the same module name would turn migration into guesswork.
- REPL latency is a fingerprinting problem, not a REPL problem. Once the toplevel stamp closes over linked archives, helper objects, toolchain facts, and env bindings, `ocamlmktop` becomes skippable on the common no-op path.
- Changed-only env reports become genuinely readable only after empty contexts disappear too. Showing a `runtime` block with no deltas is just another form of noise.
- Noninteractive REPL automation gets much less fragile once the subtool owns script transport explicitly. Feeding `oasis repl --script` through stdin avoids depending on OCaml toplevel quirks about when a trailing path is treated like a script versus just another argv.
- Editor-facing REPL support needs a plan, not just a launch command. Include dirs, linked units, env bindings, and the eventual toplevel path are the real integration surface, so `oasis repl --plan --json` is more valuable than telling users to scrape terminal output.
- Build caching is only half the job; artifact hygiene matters too. If stale `.cmi`/`.cmx` files survive a module-list change or wrapped-mode flip, `oasis install` quietly stages lies.
- File-valued CLI flags expose a completion gap immediately. Directory-mode path completion is a decent fallback for `--workspace`, but `--script` really wants a protocol branch that can hand shells full file completion again.
- Dune migration gets noticeably less annoying once composite action forms stop collapsing into “unsupported.” Translating `with-stdin-from`, `diff`, and `progn` plus tolerating alias deps keeps the migrator in the business of producing reviewable manifests instead of apology comments.
- OCaml record-label reuse is cheap until it isn't. The moment a migrator grows two `{ deps; opaque; ... }` records, explicit type annotations stop being noise and start being the difference between forward progress and compiler roulette.
- Completion protocols need to distinguish logical nouns from filesystem nouns. A single `directories` escape hatch was enough for `--workspace`, but real file flags like `repl --script` need a separate `files` mode so shell-native completion can do the right thing.
- Release artifacts should already look like something a package manager can ship. Emitting a `package/share/...` completion tree from the same generator that refreshes `docs/` and `completions/` avoids baking shell-specific install paths into every downstream recipe by hand.
- Wrapped-library compatibility is really two separate problems: the builder has to allow an intentional checked-in `Foo.ml` / `Foo.mli` wrapper, and the generated-source planner has to ignore stale wrapper artifacts from the old auto-generated path or the custom wrapper never actually takes effect.
- The safe rule for generated sources is “follow the current plan, not whatever happens to still be on disk.” Passing declared action outputs and wrapper outputs explicitly through source discovery is less clever than probing for leftover files, but it is much harder to lie to.
- Dune `with-stdin-from` and `with-stdout-to` forms only feel civilized after the manifest grows first-class `stdin_path` / `stdout` fields. Without those, every migration turns into `sh -c` noise and the builder loses track of which files are real inputs versus shell trivia.
- `progn` is the real boundary line for action structure. Flattening stdio wrappers into manifest fields removes a lot of quoting junk immediately, but truly multi-step dune rules still want a future manifest-level sequence model instead of pretending one argv is enough.
- A useful first `oasis bench` does not require a brand-new target kind on day one. Reusing executable targets with warmup/iteration controls and stable text/JSON summaries is enough to cover the common workflow now, while leaving room for dedicated benchmark metadata later if real suites demand it.
- First-class action sequences only stay honest if the sandbox model stays the same. Reusing one sandbox and one cwd across `steps = [[...], [...]]` lets later commands consume earlier scratch files without reopening the shell-quoting mess that `progn` migration was supposed to remove.
- `oasis env bench` was almost embarrassingly cheap once the env report already separated build contexts from runtime contexts. The real design win is having one target-resolution layer that every execution-flavored subtool can reuse.
- Replacing the toplevel bootstrap planner with a compiled `oasis-seed` binary fixed the worst bootstrap smell, but it exposed two follow-up costs immediately: cold-start source discovery still shells out through `ocaml scripts/render_bootstrap_mod_use.ml`, and the seed path now duplicates one full compile of the core modules before the app bootstrap build starts.
- A checked-in bootstrap-seed metadata fragment plus a sync test is a pragmatic way to remove interpreter probes from the hot path without pretending the metadata can drift safely.
- Sharing `_bootstrap/obj` between `oasis-seed` and the final app/test build only works if the generated makefile stops invalidating those objects by timestamp. Touching the shared outputs after makefile generation is a cheap fix that keeps the reuse visible and debuggable.
- The outer `make test` invocation should not eagerly include autogenerated bootstrap fragments when `bootstrap-smoke` is about to delete `_bootstrap` anyway. Doing work just to remove it a line later is exactly the kind of arbitrary friction people blame on build tools.
- A useful latency benchmark is small and phase-shaped. Recording `cold_app`, `cold_full`, and `warm_app` as stable phases is far more actionable than dumping raw `make` noise or one giant wall-clock number.
- On this machine, the default-profile bootstrap benchmark now lands around 2.3s for a cold app build, 3.3s for a cold full build, and 0.5s for a warm no-op app build, which is a concrete baseline for future regressions.

## 2026-03-29

- Wrapped-library migration has to treat an explicit dune `modules` list and a source-inferred module list the same way once a checked-in `Foo.ml` wrapper is supported. Preserving the wrapper stem literally in one path but stripping it in the other just recreates the same “special case drift” that made the feature hard to trust.
- A checked-in generated metadata fragment only stops feeling gross once the refresh path uses the real compiled planner. Keeping `scripts/render_bootstrap_mod_use.ml --format seed-metadata` around as a separate maintenance path would have preserved exactly the kind of hidden bootstrap split that build tools get blamed for.
- Shared bootstrap object reuse should be gated by actual compile compatibility, not optimism. If the common library picks up profile compile flags, env, PPX, generated sources, or preprocessed materializations, the safe move is to drop the reused objects and rebuild rather than touch stale artifacts into looking current.
- Executable-only benchmarking becomes awkward the moment one binary wants multiple benchmark personalities. A lightweight `[bench.*]` layer that points at an executable and carries argv/env/count metadata solves the real ergonomics problem without dragging benchmarks into the full build-target graph.
- Packaging got simpler as soon as the repo had one canonical staged install tree plus one install script. Letting opam and Nix both copy `package/share/...` instead of hand-encoding completion/doc paths is exactly the kind of boring consistency that keeps build tooling from feeling hostile.
- `opam lint` is opinionated in useful ways. Even for a repo-local package, using the built-in `make` variable instead of a raw `"make"` command catches packaging sloppiness early.
- The remaining blocker for GitHub Releases and Homebrew is not file copying; it is release metadata. Without one authoritative source for archive URLs, checksums, homepage/bug-report links, and license, downstream package definitions either guess or duplicate facts that will drift.
- Release metadata only counts as canonical if every downstream package definition is rendered from it. Generating both `oasis.opam` and `Formula/oasis.rb` from one shell metadata file turned packaging drift back into a normal sync test instead of a scavenger hunt.
- Source archives cannot include a Homebrew formula that embeds the source archive checksum. The formula ends up hashing itself indirectly, so the safe rule is to keep generated packaging manifests out of the archived source payload.
- Deterministic release archives need more than stable filenames. Normalizing timestamps, sorting tar entries, and disabling platform metadata like macOS copyfile sidecars are what keep checksum churn from turning release automation into cargo cult.
- A Dune replacement that still needs opam just to install itself is leaving obvious friction on the table. Reusing `make release-artifacts` plus one shared install script let GitHub Releases, Homebrew, opam, and Nix all ship the same staged tree instead of each inventing its own packaging path.
- Seed metadata only becomes reusable once it captures the library’s effective compile flags, profile env, PPX argv, and any transformed source snapshots. “Same module list” is not a strong enough notion of bootstrap compatibility once the core library stops compiling from raw checked-in files.
- `git diff --quiet` ignores untracked files, which makes it the wrong primitive for “did the tap formula change?” on a fresh checkout. `git status --short -- Formula/oasis.rb` is the safer check for a tap updater that may be creating the file for the first time.
- The Homebrew formula checksum is intentionally sensitive to any tracked-file change in the shipped source archive. That makes `make release-manifests` part of the normal edit loop for packaging-affecting work, not just something to remember at release time.
- For POSIX packaging scripts, forcing `bash script.sh` is self-inflicted friction. It leaks host bash startup quirks like bad locale config into otherwise deterministic release commands, while direct script execution stays quiet and portable.
- Feeding recursive directory paths plus individual file paths into `tar -T` creates duplicate archive entries. Deterministic release archives need a sorted file list, not a sorted recursive walk of both directories and files.
- Release archives only contain what `git ls-files` can see. A new helper script is invisible to packaging and release-cut flows until it is actually tracked, which makes source-archive tests a good backstop for missing release assets.
- Signature-only OCaml modules have to be modeled as a real source shape, not as a malformed implementation. Treating `.mli`-only stems as first-class modules let build, explain, install, and pruning stay honest without fake `.ml` stubs.
- Native interface-only libraries exposed a subtle packaging/build fact: `ocamlopt -a` will happily emit a `.cmxa` with no implementation objects, but it does not emit the companion `.a`. Expected-output logic has to follow actual implementation units instead of assuming every native archive has both files.
- Bootstrap ordering needs a per-module “primary artifact” rather than assuming every module produces `.$(OBJ_EXT)`. Chaining later rules through `.cmi` for interface-only modules kept the generated makefile deterministic without inventing dependencies on nonexistent objects.
- Dune migration only preserves signature-only APIs when `modules_without_implementation` is merged into explicit `modules` lists. Relying on source-tree inference alone still loses those stems whenever a stanza spells out its modules manually.
- Generated-file workflows stop feeling like “just use build” the moment you split them into first-class commands. `oasis action` can reuse the build planner directly, but users only get the ergonomic win if the command table, completions, docs, and env reports all learn about it too.
- An explicit promote command is only safe if it admits the current safety model. Promoting non-source outputs is straightforward; promoting generated `.ml`/`.mli` back into the tree immediately collides with the same guard that prevents checked-in/generated source ambiguity during builds.
- Packaging drift is the tax on every tracked-file change, not just release code. The Homebrew formula checksum, release docs, packaged completions, and cached bootstrap seed metadata all need to be refreshed after any user-visible CLI or source-layout change, or the test suite quite rightly treats the repo as inconsistent.
- A generated source can safely coexist with a checked-in snapshot only if the manifest makes that intent explicit. A dedicated `checked_in_sources` field keeps the normal collision guard strict while still supporting promote-style workflows.
- Action sandboxes should start from a world where declared outputs do not exist. Otherwise a copied checked-in snapshot can trick the build into “regenerating” a file the action never actually wrote.
- Once generated snapshots are a first-class mode, bootstrap input tracking has to follow the real compile path rather than the logical module path. Tracking the checked-in file as an input while compiling the generated copy would make self-hosting fingerprints lie.
- `oasis init` only feels honest if the generated tree runs immediately. A scaffolder that still needs hand edits is just moving boilerplate around.
- A useful first lock file does not need a dependency solver. Capturing manifest digests, resolved toolchain paths, backend choice, and `ocamlfind` package roots already makes dependency state reviewable.
- Vendoring is mostly a manifest-edit problem, not a copy problem. The sharp edge is keeping vendored packages member-safe and updating `members = [...]` without forcing manual surgery in the root manifest.
- Packaging metadata is part of the normal edit loop. Any tracked-file change that affects the source archive means the generated Homebrew checksum becomes stale immediately.

## 2026-03-29

- Lock validation needs both workspace-wide package-path checks and per-target closure checks, and the tests have to mutate the specific `package_paths` entries they mean to exercise. A naked string replacement against `unix` can accidentally hit recorded toolchain fields instead of the locked dependency graph.
- `oasis ppx` is a source-inspection tool, not a runtime evaluator. If a test wants to prove “preprocessor plus PPX,” the PPX fixture has to rewrite the post-preprocess syntax tree the compiler actually sees rather than expecting runtime-style string concatenation from a pretty-printed source dump.
- The bootstrap path is much better than the old hard-coded object lists, but `scripts/bootstrap_seed_metadata.mk` is still a visible maintenance seam whenever core-module ordering changes. Anything that still requires a tracked metadata refresh is a real source of friction, not just cleanup debt.
- Lock snapshots only become trustworthy once they validate toolchain identity as well as package paths. Compiler version, stdlib roots, unix roots, and `ocamlfind` search roots all drift independently of a target’s declared package closure.
- Remote vendoring has to be pin-first and lifecycle-aware. A temp checkout or extracted archive cannot outlive the copy step that consumes it, and a checksum verifier that inherits a bad locale is just another source of nondeterminism.
- GNU make can smooth a bootstrap seam if the included metadata file knows how to remake itself from a compiled planner and preserves its timestamp when the rendered content is unchanged. That removes the manual refresh tax from iterative workflows even before the clean-checkout story is fully solved.
- Shared PPX/preprocess fixture helpers matter because transform tests go stale fastest when every suite hand-inlines its own rewriter source. One marker-rewriter helper keeps the tests talking about real rewritten syntax instead of four slightly different toy PPX programs.
- First-build bootstrap cache belongs under `_bootstrap`, not `scripts/`. If a build artifact dirties the source tree, the tool is still exporting internal plumbing to the user.
- GNU make will happily start from empty included-variable values if a missing metadata file is only optional. For clean-checkout bootstrap, the missing metadata has to be materialized before `include` or the seed compile path degenerates into a noisy false start.
- The bootstrap story is materially better once the repo no longer tracks seed metadata, but there is still one honest follow-up left: the clean-checkout path uses the OCaml toplevel once to mint the first metadata file, and that fallback is the next seam to shrink.
- A metadata-only bootstrap helper only needs a small slice of the manifest model to pay off: library source discovery, `ocamldep -sort`, default-profile flag/env merging, and seed-root snapshotting cover the common self-hosting case without loading the full planner into the toplevel.
- GNU make's included-makefile remake semantics are not enough on their own when the same build also treats the generated metadata as ordinary prerequisites. The clean bootstrap path needed an explicit refresh-and-reenter guard to avoid one bogus empty-variable `oasis-seed` compile.
- Generated artifact drift is much easier to live with once one target refreshes everything that actually ships. Folding bootstrap metadata, docs, completions, and packaging manifests under `make sync-generated` turned the stale Homebrew checksum failure back into a single-command maintenance step instead of a scavenger hunt.
- A recursive `make` that is only trying to build `oasis-seed` should not also drag the generated app/full bootstrap makefiles into scope. Keeping the seed self-build path separate from generated-makefile includes removes an entire false-start planning pass from clean full bootstraps.
- Bootstrap shared-output reuse decisions have to read live metadata, not whatever variable values the parent `make` happened to parse before a restart. Moving the touch/prune step behind a helper that consumes the regenerated seed metadata closed the last visible “empty loop” seam in clean-checkout logs.
- The Homebrew formula checksum is a checksum of the current tracked source tree, so even plan/journal edits invalidate it. That means packaging-manifest refresh really is part of normal development hygiene, not just release day ceremony.
- Packaging manifest generation is much easier to trust when it can either reuse a caller-provided source archive or refresh a retained `dist/` tarball directly. Throwaway temp archives hide the exact bytes behind the Homebrew checksum and encourage duplicate work.
- Shell-only maintenance targets should opt out of bootstrap include machinery. If `make release-manifests` has to compile bootstrap planners before it can run a shell script, the tool is still exporting build-system friction onto routine packaging upkeep.
- Release workflows drift the moment they render formulas or checksum files outside the shared packaging script. The manifest generator has to own both the source-archive reuse decision and the final asset layout or CI quietly becomes a second release tool.
- `--source-archive-dir` only becomes useful for CI once it means “reuse the expected archive if it is already here, rebuild it otherwise.” Treating that flag as an unconditional rebuild knob defeats the whole point of downloading release artifacts once.
- Repo layout and release-asset layout are different concerns. A packaging generator that can target either `Formula/oasis.rb` or `dist/oasis.rb` from the same code path removes another small but recurring source of publishing drift.
- If `oasis.opam` is going to be a first-class release asset, the packaging generator has to treat its path the same way it treats the formula path. Hard-wiring one metadata file to `OUTPUT_DIR/oasis.opam` while the other accepts an explicit destination is exactly the kind of asymmetric release plumbing that turns maintenance into guesswork.
- Publishing extra metadata files without checksums is only half a release story. The moment `oasis.opam` and `oasis.rb` become downloadable assets, `SHA256SUMS` has to cover them too or the release integrity story silently bifurcates between archives and metadata.

## 2026-03-29

- A release asset index only earns its keep if it is generated from the same script that renders `oasis.opam`, `oasis.rb`, and `SHA256SUMS`. The minute the workflow writes a second metadata file by hand, release automation is back to scraping incidental state.
- `--source-archive PATH` was only half-finished until checksum and asset-index generation respected archives that live outside `OUTPUT_DIR`. Reuse knobs that silently disappear from downstream metadata are just another flavor of build-tool dishonesty.
- Publishing machine-readable release metadata is only useful if local maintenance paths exercise it too. Wiring `release-assets.json` through `make release-manifests` and `cut_release` keeps the release workflow from becoming a one-off branch of logic that nobody runs until tag day.
- The Homebrew formula checksum remains a checksum of the whole tracked source tree. Any tracked edit that lands before a packaging refresh, including plan or journal updates, is enough to invalidate the formula and should be treated as normal packaging drift rather than a surprising failure mode.

## 2026-03-29

- `oasis status` only became worth adding once it reused `Builder.explain_current` instead of inventing a second planner. A summary command is trustworthy when it is just another renderer over the same build facts.
- A `doctor` command is most useful when it degrades lock issues to warnings by default but can still turn them fatal on demand. Diagnosis and enforcement are the same data with different exit semantics.
- Portable watch mode does not need platform-specific filesystem APIs to be useful. A polling snapshot is good enough as long as it ignores build artifacts, preserves child exit semantics, and can keep going after failures when the user asks.
- The packaging tests were right to fail as soon as the command table changed. If a single new subtool invalidates the CLI docs and Homebrew checksum, that is evidence that generated release assets are part of the feature surface, not downstream chores.

## 2026-03-29

- `oasis watch` only feels like a wrapper, not a second-class subtool, once completion delegates straight back into the selected inner command. Reusing the existing completion engine beats cloning target/flag logic into a watch-specific branch.
- Polling snapshots can support include/ignore globs without trusting directory mtimes as invalidation signals. Recording directories as structural markers while filtering files by glob keeps unrelated tree churn from triggering reruns.
- Maintenance drift checks should execute the same release-generation scripts the repo already ships, not a hand-maintained reimplementation. The useful signal is whether regenerated docs/completions/metadata byte-match the committed artifacts.
- Reinvoking the current binary through `Sys.executable_name` is not reliable when the command came from `PATH`. Self-hosted commands like `watch` and generator-backed `doctor` checks need an explicit executable resolver instead of assuming `argv[0]` is cwd-relative.

## 2026-03-29

- Persistent watch policy needs a tolerant root-only parser. Forcing `oasis watch` to fully load a broken workspace before it can start would sabotage the exact edit-fix loop watch mode is meant to preserve.
- Shared watch filters and local noise filters are different kinds of state. `[watch]` belongs in reviewed manifest config, while `.oasiswatchignore` is a better home for per-repo churn like docs or vendored trees that should not keep leaking into shell history.
- Once `oasis watch` delegates back into `oasis`, an inner `--workspace` flag becomes a second repository selector. If the polled tree and the delegated subtool can drift apart, the watcher is lying about what it is observing.

## 2026-03-29

- A watcher needs two views of the tree: control files that mutate watch policy, and selected inputs that should actually trigger reruns. Merging those sets makes `.oasiswatchignore` edits look like source changes or disappear behind include globs.
- Root control files have to bypass ordinary include-glob pruning on purpose. If `oasis watch` can hide `oasis.toml` or its ignore file from itself, config edits become another round of build-tool superstition.
- Bad local watch-policy edits should degrade to warnings plus “last known good” behavior. Killing the poll loop because `.oasiswatchignore` briefly became a directory is exactly the kind of hostile friction Dune has already trained people to expect.

## 2026-03-29

- `oasis.lock` is not just another source file and not quite a watch-policy file either. The watcher needed an explicit split between “reload policy when this changes” and “rerun the delegated subtool when this changes,” or lockfile edits would either disappear behind include globs or inherit the wrong semantics from `.oasiswatchignore`.
- Narrow watch include globs are only trustworthy if command-critical root files bypass them on purpose. A user who says “watch `app/**`” still expects `oasis watch build --locked` or `oasis watch doctor` to notice that the root lock snapshot changed.
- Printing the watched root-file set up front is worth the noise. Hidden watch inputs are exactly the kind of build-tool magic that turns a legitimate optimization into superstition.

## 2026-03-29

- Watch-specific filesystem requirements belong in the same command metadata as help text and completions. If lock awareness lives in a watcher-side `match command_name` branch, the next root-level input drift is already scheduled.
- A flat `Watch-root-files` list is not enough once files have different semantics. Showing `reload+rerun`, `reload-only`, and `rerun-only` roles turns watch behavior from folklore into something a user can verify at startup.
- Packaging checksum drift is the tax on tracked-source changes, including plan and journal edits. The formula refresh is not downstream housekeeping; it is part of closing the loop on a change set that ships source archives.

## 2026-03-29

- Release maintenance got noticeably less brittle once `release/metadata.sh` stayed a flat shell-compatible fact file and the OCaml CLI learned to parse it directly instead of re-encoding those constants in a second language.
- Packaging checksum workflows are only as honest as their source-archive input set. If archive generation is `git ls-files`-driven, newly added files do not exist for the release pipeline until they are staged, which is worth treating as an explicit policy choice rather than accidental magic.
- Clean-checkout packaging tests need a deliberate story for in-progress untracked source files. Simulating a repo by copying only tracked paths is correct for release bytes, but local development tests still need a narrow escape hatch or new modules disappear from the fixture before they ever reach git.
